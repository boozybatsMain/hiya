# Пересборка рекламы — очередь задач (TODO)

Задачи, которые выполняются при **следующей пересборке** кампании в Meta Ads.
Копим их здесь, а не правим кампанию по одной, потому что почти любое изменение
адсета (гео, плейсменты, событие оптимизации, бюджет >±20%) перезапускает фазу
обучения — см. «Известные грабли» в [`meta-pixel.md`](meta-pixel.md). Все правки
публикуются одним пакетом.

## Протокол для агента

1. **Перед пересборкой** (или любой пакетной правкой кампании/адсета) открой этот
   файл и **спроси пользователя в чате, какие задачи из списка выполнять**.
   Без явного «да» по задаче — не выполнять.
2. Дальше действуют правила [`CLAUDE.md`](../CLAUDE.md): показать, что именно
   будет сделано, получить подтверждение, опубликовать одним пакетом.
3. Выполненное перенеси в «Журнал» внизу (дата, что сделано, комментарий).
4. Появилась новая идея, требующая правки кампании, — **добавь сюда задачу**
   (что + зачем + дата), а не применяй сразу. Текущий `adset-01-broad` между
   пересборками не трогаем.

## Задачи на следующую пересборку

- [x] ~~**Гео: город Алматы + радиус 20–40 км** вместо «Алма-Ата Almaty Region».~~
  **ЗАКРЫТО 15.07.2026 — менять НЕ нужно, гипотеза опровергнута данными.**

  > ⛔️ **Агент, стоп.** Если ты сейчас смотришь на `Казахстан: Алма-Ата Almaty
  > Region` в адсете и думаешь «слово Region — значит это область, надо
  > поправить на город» — **не надо**. Ровно так рассуждали три агента подряд,
  > и каждый лез перенастраивать гео. Это city-сущность, а «Almaty Region» в
  > подписи — просто регион, в котором город числится. Проверено фактом:
  >
  > GA, платный трафик за 12–15.07 (после отключения Advantage+ locations),
  > 193 сессии: **Almaty — 184 (95,3%)**, (not set) — 3, Taldykorgan — 2,
  > Kokshetau / Konaev / Tashkent / Tekirova — по 1.
  >
  > Талдыкорган и Конаев — как раз города Алматинской области. Ловил бы таргет
  > область, их доля была бы заметной; вместо этого 3 сессии из 193 — обычная
  > погрешность геолокации по IP. 95,3% попадания в город — норма для Meta.
  > Перепроверить можно так: `scripts/ga_report.sh` с dimension `city` и
  > фильтром `sessionMedium=paid`.

  Что действительно важно и остаётся в силе: в **новом** адсете заново снять
  галочку **Advantage+ locations («Оптимизируйте местоположения»)** — Meta
  включает её по умолчанию в каждом новом адсете, и вот она-то и размазывала
  показы по всему Казахстану (см. `meta-pixel.md`). Сейчас в `adset-01-broad`
  она выключена. _(добавлено 11.07.2026, закрыто 15.07.2026)_
- [ ] **Блокер перед публикацией: разрешение на dating-рекламу Meta.** Сайт
  позиционируется как «знакомства», по политике Restricted Goods & Services
  нужна предварительная заявка (Business Help Center → dating onboarding);
  пересборка = новая модерация всех креативов, самый вероятный триггер
  классификации. Фолбэк, пока заявка рассматривается: вариант креатива и копии
  в рамке «social discovery», без слова «знакомства». _(добавлено 11.07.2026)_
  **Статус: заявка подана 12.07.2026** через форму
  `facebook.com/help/contact/1060028634014002`, обращение
  `122097704349396498` (ответ и вопросы Policy-команды — в тредe
  facebook.com/support и на почту из заявки; срок ответа — до 30 дней).
  Задача закрывается, когда придёт письменное разрешение.
- [ ] **Цель «Лиды», оптимизация на кастомную конверсию OpenSignup.**
  Подготовка сделана 12.07.2026: кастомная конверсия «OpenSignup» создана в
  Events Manager (датасет `2523353561436955`, категория «Лид», правило
  URL содержит `boozybatsmain.github.io`); колонки OpenSignup + Лиды
  (кол-во и цена) добавлены в пресет «Hiya MVP» в Ads Manager.
  Осталось на пересборку: новый адсет с целью «Лиды» и оптимизацией на эту
  конверсию. На оптимизацию по Lead переходить одной правкой при
  ≥30–50 лидов/нед. _(добавлено 11.07.2026)_
- [ ] **Кастомная конверсия OpenSignup: считается, но НЕ атрибуцируется к
  объявлениям — лечится самой пересборкой.** Разбор 18.07.2026 (глубже, чем
  запись от 14.07): конверсия ЖИВА и считает (13 шт. за 12–17.07, ID
  `1546100047000672`, колонка в пресете привязана к правильному ID), 11–12 из
  13 опенов — от платных сессий (GA, sessionSourceMedium=ig/paid), склейка
  клик→человек у Меты работает (368 атрибуцированных «Просмотров целевой» у
  creative-01 за тот же период). Но в колонках объявлений НОЛЬ во всех шести
  окнах атрибуции (проверено экспортом CSV со сравнением окон).

  **ПРИЧИНА НАЙДЕНА И УСТРАНЕНА 18.07.2026.** Кастомное событие OpenSignup
  висело в Events Manager → Настройки → «Управление блокировкой событий» со
  статусом **«Требуется действие: подтвердить или заблокировать»**. Формулировка
  Меты дословно: «Без вашего подтверждения и одобрения специально настроенные
  события нельзя использовать, чтобы создавать пользовательские конверсии,
  пользовательские аудитории и рекламные кампании». То есть событие считалось
  в датасете, но для рекламных функций (атрибуция в колонках, оптимизация,
  аудитории) не использовалось. 18.07 событие подтверждено (статус
  «Подтверждённые: 1», владелец дал добро в чате) — новые опены должны начать
  клеиться к объявлениям; пересчёт старых 13 задним числом не гарантирован.
  Урок на будущее: **каждое новое trackCustom-событие после первой отправки
  надо подтверждать в этом же меню**, иначе оно «невидимо» для кабинета.
  Для нового адсета с оптимизацией на OpenSignup — препятствий больше нет;
  через сутки после подтверждения проверить, что колонка ожила (новые опены).
  Контроль-перекрёстка: колонка «Лиды» (стандартное событие, подтверждения
  не требует + CAPI с 18.07) должна наполняться независимо.
  _(добавлено 14.07.2026; решено 18.07.2026)_
- [ ] **Проверить URL-параметры у creative-02d-hero-honest / creative-02c-hook-phone.**
  Факт 18.07.2026: у creative-02d в кабинете 25 кликов и 18 просмотров целевой
  страницы, но в GA нет НИ ОДНОГО события с `ft_content=creative-02d-hero-honest` —
  весь трафик 16–18.07 идёт с `ft_content=creative-01-dark-phone`. Похоже, у
  нового объявления не проставлен UTM-шаблон (или проставлен не тот). Сначала
  просто посмотреть поле «Параметры URL» объявления (это не правка); если пусто —
  добавить шаблон из чек-листа ниже пакетом с пересборкой (правка объявления =
  повторная модерация). _(добавлено 18.07.2026 по итогам разбора «0 лидов в кабинете»)_
- [ ] **Единый UTM-шаблон на всех объявлениях:**
  `?utm_source=ig&utm_medium=paid&utm_campaign={{campaign.name}}&utm_content={{ad.name}}&utm_term={{adset.name}}`;
  имена кампании/адсетов/объявлений — ascii-слаги (`leads-alm-01`,
  `creative-02-...`). _(добавлено 11.07.2026)_
- [ ] **Старые кампании на паузу** одновременно с запуском новой — не дробить
  бюджет $10/день между кампаниями. _(добавлено 11.07.2026)_
- [ ] **Плейсменты вручную: только Instagram** (лента / сторис / Reels) и
  выключить «Ограниченные затраты на исключённые места размещения».
  Приоритет низкий: утечка на FB сейчас копеечная (3 просмотра лендинга за всё
  время). _(добавлено 11.07.2026)_
- [ ] **Креативы из постов Instagram (Use existing post).** Профиль наполнен
  6 постами в фирменном стиле (файлы: `~/Desktop/hiya-instagram/`, тексты:
  `docs/instagram-posts.md`). В новом адсете попробовать объявления из
  существующих постов — манифест и карусель «Как это работает» первые
  кандидаты: соцактивность копится на посте, оптимизация и UTM как обычно.
  Кнопку «Продвигать» (Boost) в приложении не использовать — она оптимизирует
  под охват, а не под Lead. _(добавлено 12.07.2026)_

## Чертёж пересборки — имена и UTM (заготовка, править тут)

Все имена — ascii-слаги: они через макросы `{{…}}` попадают в UTM и дальше в GA;
кириллица/пробелы дадут кракозябры в отчётах. Имена фиксируются ДО создания
сущностей — переименование после публикации рвёт историю в GA.

| Сущность | Имя | Примечание |
|---|---|---|
| Кампания | `leads-alm-01` | цель «Лиды» |
| Адсет | `adset-01-alm-broad` | Алматы +20–40 км, 20–35, IG-плейсменты вручную, Advantage+ locations выкл, оптимизация на кастомную конверсию OpenSignup |
| Объявление | `creative-01-dark-phone` | текущий креатив (перенос) |
| Объявление | `creative-02c-hook-phone` | **основной кандидат на A/B против `creative-01`.** Готов: `~/Desktop/hiya-instagram/ads/creative-02c-hook-phone.jpg` (v2 от 12.07). Хук старого креатива («Не гадай, можно ли подойти»), без подтайтла — ответ даёт сам телефон. На экране 3D-телефона — лента с резкими лицами и «Лайк», сверху крупная плашка-соцдоказательство «9 человек готовы к знакомству в этом заведении» (увеличенный vis-toast, правки только в кадре). CTA — плашка: «Стань первым из 300» (золотом) / «Ранний доступ открыт • бесплатно». Архитектура связки: реклама задаёт вопрос-боль → первый экран лендинга отвечает сценарием → мокап в объявлении = мокап на лендинге. Исходник `.src.html` и телефоны с прозрачным фоном (`phone-mockup-3d-intro.png`, `phone-mockup-3d.png`) рядом |
| Объявление (запас) | `creative-02b-hero-almaty-phone` | вариант со сценарным заголовком вместо хука | Готов: `~/Desktop/hiya-instagram/ads/creative-02b-hero-almaty-phone.jpg` (1080×1350). Гибрид: фон = фото лендинга, заголовок = H1 лендинга дословно, пилюля «Алматы», телефон с реальным UI лендинга (лица + «Лайк») — за полсекунды читается «это приложение». «Ранний доступ открыт — займи место · бесплатно», без «Премиум навсегда» (эксклюзив модалки). Исходник-вёрстка `.src.html` рядом; вырезанный мокап — `phone-mockup-feed.png`. Вариант без телефона (`creative-02-hero-almaty.jpg`) — запасной, скорее не запускать: нет продукта в кадре. A/B-гипотезы: creative-01 «любопытство → максимум кликов» vs 02b «честные ожидания → меньше кликов, чище трафик»; сравнивать не CTR, а связку CTR → доля уходов ≤2 с → open_signup → лид по utm_content |
| Объявление | `creative-03-manifest` | Use existing post: пост-манифест |
| Объявление | `creative-04-carousel-how` | Use existing post: карусель «Как это работает» |
| Объявление (резерв) | `creative-05-social-discovery` | фолбэк без слова «знакомства» — если Meta не даст разрешение на dating |

UTM-шаблон (одна строка во ВСЕ объявления, поле «Параметры URL»):

```
utm_source=ig&utm_medium=paid&utm_campaign={{campaign.name}}&utm_content={{ad.name}}&utm_term={{adset.name}}
```

Как это будет выглядеть в GA после подстановки:
`utm_campaign=leads-alm-01`, `utm_content=creative-03-manifest`,
`utm_term=adset-01-alm-broad` — источник/кампания/креатив читаются без декодера.

Чек-лист публикации: старые кампании на паузу тем же пакетом; в новом адсете
снять Advantage+ locations и «Ограниченные затраты на исключённые места
размещения»; проверить, что событие оптимизации — кастомная конверсия
OpenSignup; Boost из приложения не использовать.

## Журнал выполненного

| Дата | Что сделано | Комментарий |
|---|---|---|
| 11.07.2026 17:31–17:34 | В `adset-01-broad` выключен Advantage+ locations, возраст 20–35, ручные плейсменты Instagram | сделано владельцем вручную (вне этого списка); публикация подтверждена в истории изменений адсета |
| 12.07.2026 20:25 | Подана заявка на dating-рекламу Meta (обращение `122097704349396498`) | агент заполнил форму, владелец поправил имя/почту и подтвердил отправку; ждём ответ Policy до 30 дней — сам блокер остаётся открытым до письменного разрешения |
| 12.07.2026 | Создана кастомная конверсия «OpenSignup» (Events Manager, категория «Лид», URL содержит `boozybatsmain.github.io`); в пресет столбцов «Hiya MVP» добавлены OpenSignup и Лиды (кол-во + цена) | адсет не трогали — обучение не сброшено; конверсия начнёт считать события с момента создания |
| 13.07.2026 | Дневной бюджет кампании `hiya-traffic-01` снижен $10 → $5 (Advantage+ campaign budget, одна правка) | −50% > порога ±20% — обучение сброшено осознанно: адсет доживает до пересборки, экономим в ожидании ответа Meta. Гео и прочие правки таргетинга НЕ делали — риск повторной модерации при висящей dating-заявке. Публикация подтверждена («Кампания обновлена») |
